# 🦉 OWL-AGENT PROXY DEFENSE STACK — Pinned Dependencies
# Start with http/1.1. Escalate on demand.

# ─── CORE (Phase 1: Always Install) ─────────────────────────────────────────
aiohttp==3.11.18
aiohttp-socks==0.10.1
aiofiles==24.1.0
cachetools==5.5.2
expiringdict==1.2.2
PySocks==1.7.1
requests[socks]==2.32.3
urllib3==2.4.0
PyYAML==6.0.2
python-json-logger==3.3.0
orjson==3.10.16
certifi==2025.1.31
cryptography==44.0.2
pyOpenSSL==25.0.0
tenacity==9.1.2
fake-useragent==2.2.0
python-dateutil==2.9.0

# ─── OPTIONAL (Phase 2-5: Install on Demand) ──────────────────────────────
# httpx[http2]==0.28.1      # HTTP/2 support
# h2==4.2.0                  # HTTP/2 protocol
# curl_cffi==0.10.0          # JA3 fingerprint spoofing
# websockets==15.0.1         # WebSocket support
# playwright==1.52.0           # Browser automation
